# Sistema Comparador de Precios — Santander Boutique
#
# Instalación completa:
#   pip install -r requirements.txt
#   python -m camoufox fetch       ← descarga el browser (obligatorio, solo 1 vez)
#   python -m patchright install chromium  ← instala chromium para StealthyFetcher

# Core scraping
scrapling[fetchers]>=0.4

# TLS fingerprinting (usado por Fetcher HTTP)
curl_cffi>=0.7.0

# Anti-bot stealth browser (Samsung Store, Amazon)
camoufox[geoip]>=0.4
patchright>=1.0.0

# Excel export
openpyxl>=3.1.0

# Product name fuzzy matching
rapidfuzz>=3.0.0

# Logging
loguru>=0.7.0
